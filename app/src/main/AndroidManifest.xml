<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED"/>
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwapProject"
        tools:targetApi="31">

        <activity
            android:name=".newChaletActivity"
            android:exported="false"
            android:label="@string/title_activity_new_chalet"
            android:theme="@style/Theme.SwapProject" />
        <activity
            android:name=".MyChaletsListView"
            android:exported="false"
            android:label="@string/title_activity_my_chalets_list_view"
            android:theme="@style/Theme.SwapProject" />
        <activity
            android:name=".activities.UserProfileActivity"
            android:exported="false"
            android:label="@string/title_activity_user_profile"
            android:theme="@style/Theme.SwapProject" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.CreateAccountActivity"
            android:theme="@style/Theme.SwapProject" />
        <activity
            android:name=".activities.UserSessionActivity"
            android:theme="@style/Theme.SwapProject" />
    </application>

</manifest>